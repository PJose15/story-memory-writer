'use client';

// Re-export from the antiquarian library shell
// The LibraryShell includes StoryProvider, SessionProvider, ToastProvider,
// ConfirmProvider, ParchmentSidebar, and DiagnosticGate.
export { LibraryShell as AppShell } from '@/components/antiquarian/library-shell';
