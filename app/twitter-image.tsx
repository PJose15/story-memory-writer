// Twitter card image — reuses the OpenGraph image composition.
// Next.js convention picks this up automatically for `twitter:image`.
export { runtime, alt, size, contentType, default } from './opengraph-image';
